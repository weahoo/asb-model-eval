# Security and public-data policy

The public harness contains synthetic paths, synthetic addresses and mock schemas. It must not contain API keys, access tokens, customer data, machine identifiers, private screenshots, temporary tunnel URLs or private ASB logs.

Never commit result files before reviewing them. The results directory is ignored by default. Redact endpoint query parameters and authorization data before publishing any report.
