# ASB lifecycle benchmark

Report expert_ui, model_direct, model_asb_api, model_asb_ui and assisted separately. A pass requires every gate, a real package, and two non-default executions with verified external effects. Exit code zero alone is insufficient. Record every retry and intervention; never delete business inputs to bypass wiring validation.
